# 12B extractor variant, validated by Fable 2026-09-11 13:00 PDT through Cognee on record ff49d50a:
# add 0.8 s, cognify 69.2 s (graph + summary), graph search 0.3 s, zero json_invalid retries.
# Fully local Cognee config (Fable, 2026-09-11 12:35 PDT).
# LLM: Ollama's OpenAI-compatible /v1 endpoint. The model name "gpt-4o-mini" is an
# Ollama alias of gemma3:27b (`ollama cp gemma3:27b gpt-4o-mini`), chosen so that
# litellm.supports_response_schema() returns True and Cognee takes its native
# json_schema path (Ollama enforces the schema by grammar). Nothing leaves the machine.
TELEMETRY_DISABLED=true
CACHING=false
LLM_PROVIDER=openai
LLM_MODEL=gpt-4o   # Ollama alias of gemma3:12b (digest f4031aab637d): ollama cp gemma3:12b gpt-4o
LLM_ENDPOINT=http://localhost:11434/v1
LLM_API_KEY=ollama
EMBEDDING_PROVIDER=openai_compatible
EMBEDDING_ENDPOINT=http://localhost:8080/v1   # any OpenAI-compatible embedder (ours: HF text-embeddings-inference)
EMBEDDING_MODEL=unsloth/embeddinggemma-300m
EMBEDDING_DIMENSIONS=768
EMBEDDING_API_KEY=local
LLM_MAX_COMPLETION_TOKENS=8192
COGNEE_SKIP_CONNECTION_TEST=true
